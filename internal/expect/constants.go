package expect

const (
	formatMismatch        = "got %#v, want %#v"
	formatUnexpectedError = "unexpected error: %v"
	formatWrongError      = "got error %v, want an error matching %v"
	formatFalse           = "expected %s"
)
